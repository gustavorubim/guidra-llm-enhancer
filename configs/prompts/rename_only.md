You are generating only a rename map from binary-grounded evidence.

Return strict JSON with keys:
- summary
- confidence
- renamings
- cleaned_c

Set `cleaned_c` to the original decompiled code if you cannot justify structural cleanup.
