# Windows CUDA Validated Environment

This file is the pinned-environment note referenced by the spec. Update it after the first successful native Windows CUDA SFT/GRPO smoke run with:

- OS build
- Python version
- GPU model and VRAM
- torch, unsloth, trl, transformers, datasets, accelerate versions
- confirmed `configs/training/*` profile
